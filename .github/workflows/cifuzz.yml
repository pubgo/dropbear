# Runs fuzzers for a little while.
# From https://google.github.io/oss-fuzz/getting-started/continuous-integration/
# Not on pull_request: the oss-fuzz image clones hg.ucc.asn.au/dropbear-fuzzcorpus,
# which frequently aborts mid-stream and is outside our control.
name: CIFuzz
on:
  workflow_dispatch:
  push:
    branches:
      - master
# Cancel superseded runs on the same ref so pushes don't pile up.
concurrency:
  group: cifuzz-${{ github.ref }}
  cancel-in-progress: true
jobs:
  Fuzzing:
    runs-on: ubuntu-latest
    steps:
      - name: Build Fuzzers
        id: build
        uses: google/oss-fuzz/infra/cifuzz/actions/build_fuzzers@master
        with:
          oss-fuzz-project-name: "dropbear"
          dry-run: false
      - name: Run Fuzzers
        uses: google/oss-fuzz/infra/cifuzz/actions/run_fuzzers@master
        with:
          oss-fuzz-project-name: "dropbear"
          fuzz-seconds: 300
          dry-run: false
      - name: Upload Crash
        uses: actions/upload-artifact@v4
        if: failure() && steps.build.outcome == 'success'
        with:
          name: artifacts
          path: ./out/artifacts
